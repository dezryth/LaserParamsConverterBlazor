Laser
<DxComboBox Data="@Lasers" @bind-Value="@SharedState.Laser">
</DxComboBox>
@Value

@code {
    [Inject]
    private SharedStateService? SharedState { get; set; }

    public LaserType Value { get; set; }

    public IEnumerable<string> Lasers = Enum.GetNames(typeof(LaserType));
}

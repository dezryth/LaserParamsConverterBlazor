Laser
<DxComboBox Data="@Lasers" @bind-Value="@Value">
</DxComboBox>
@Value

@code {
    [Inject]
    private SharedStateService? SharedState { get; set; }

    public LaserType Value { get; set; }

    public IEnumerable<string> Lasers = (IEnumerable<String>)Enum.GetValues(typeof(LaserType));
}

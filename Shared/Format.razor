Format
<DxComboBox Data="@Formats" @bind-Value="@SharedState.Format"></DxComboBox>

@code {
    [Inject]
    private SharedStateService? SharedState { get; set; }

    public IEnumerable<string> Formats { get => Enum.GetNames(typeof(LibraryType)); set { } }
}
